# Shared SWUIFT workspace environment
#
# Setup:
#   cd doe-wildfire
#   python3 -m venv .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt

# --- Shared physics core (install first) ---
-e packages/core
-e packages/cli

# --- Core simulation (app, CLI, orchestrator) ---
numpy
scipy
h5py
numba
matplotlib
imageio[ffmpeg]
tqdm
av

# --- Desktop app GUI ---
PySide6>=6.6
Pillow

# --- Tests ---
pytest

# --- App packaging (PyInstaller builds) ---
pyinstaller>=6.0
